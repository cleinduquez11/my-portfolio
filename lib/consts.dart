import 'package:flutter/material.dart';

const primaryColor = Color(0xFF2f4f4f);
const secondaryColor = Color(0xFF87cefa);
const darkColor = Color(0xFF000000);
const bodyTextColor = Color(0xFFFFFFFF);
const bgColor = Color(0xFFf5f5f5);
const double titleSize = 60;
const double semititleSize = 48;
const double smalltitleSize = 24;
const lightColor = Color.fromARGB(255, 122, 216, 216);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web